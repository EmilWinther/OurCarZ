@page
@model OurCarZ.Pages.AdminStatsModel
@{
}

<p>How'd you get here?</p>
@foreach (var item in Model.RouteList)
{
    <p>
    @item.FinishPoint
    @item.StartPoint
    @item.UserId
    </p>
}
@foreach (var itemz in Model.AddressIdToRoadName)
{
<p>
    @itemz.Key
    @itemz.Value
</p>
}

        <html>
        <head>
            <!--Load the AJAX API-->
            <script type="text/javascript" src="https://www.gstatic.com/charts/loader.js"></script>
            <script type="text/javascript">

                // Load the Visualization API and the corechart package.
                google.charts.load('current', { 'packages': ['corechart'] });

                // Set a callback to run when the Google Visualization API is loaded.
                google.charts.setOnLoadCallback(drawChart);


                // Callback that creates and populates a data table,
                // instantiates the pie chart, passes in the data and
                // draws it.
                function drawChart() {

                    // Create the data table.
                    var data = new google.visualization.DataTable();
                    data.addColumn('string', 'Orders');
                    data.addColumn('number', 'Amount');
                    data.addRows([
                        ['Order Guide You', @Model.AddressIdToRoadName.Keys.Count],
                        ['Order Guide You2', @Model.RouteList.Count]
                ]);

                // Set chart options
                var options = {
                    'title': 'How Many Orders Delivered / Orders Still Not Delivered',
                    'width': 400,
                    'height': 300
                };

                // Instantiate and draw our chart, passing in some options.
                var chart = new google.visualization.PieChart(document.getElementById('chart_div'));
                chart.draw(data, options);
}
            </script>
    </head>
    <body>
        <!--Div that will hold the pie chart-->
        <div id="chart_div"></div>
        <!--Div that will hold the pie chart-->
        <div id="chart_div1"></div>
    </body>
    </html>
    